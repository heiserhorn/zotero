Index: zotero-standalone-build/modules/zotero-libreoffice-integration/resource/installer.jsm
===================================================================
--- zotero-standalone-build.orig/modules/zotero-libreoffice-integration/resource/installer.jsm	2012-11-30 14:11:36.000000000 +0100
+++ zotero-standalone-build/modules/zotero-libreoffice-integration/resource/installer.jsm	2012-12-27 17:50:42.000000000 +0100
@@ -187,13 +187,19 @@
 				if(success) {
 					zpi.success();
 				} else {
-					openInstallationWizard();
+/*					openInstallationWizard();
+*/
+                                        zpi.success();
+
 				}
 			});
 		} else if(!zpi.failSilently || this.getPotentialInstallations().length) {
 			// otherwise, if there are installations and we are not failing silently, open the
 			// wizard
-			openInstallationWizard();
+/*			openInstallationWizard();
+*/
+                                        zpi.success();
+
 		}
 	}
 	
@@ -353,4 +359,4 @@
 			}});
 		}});
 	}
-}
\ No newline at end of file
+}
