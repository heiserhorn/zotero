Index: zotero-standalone-build/modules/zotero-libreoffice-integration/resource/installer.jsm
===================================================================
--- zotero-standalone-build.orig/modules/zotero-libreoffice-integration/resource/installer.jsm	2013-03-28 17:24:03.000000000 +0100
+++ zotero-standalone-build/modules/zotero-libreoffice-integration/resource/installer.jsm	2013-03-28 17:24:03.000000000 +0100
@@ -191,13 +191,19 @@
 				if(success) {
 					zpi.success();
 				} else {
-					openInstallationWizard();
+/*					openInstallationWizard();
+*/
+                                        zpi.success();
+
 				}
 			});
 		} else if(!zpi.failSilently || this.getPotentialInstallations().length) {
 			// otherwise, if there are installations and we are not failing silently, open the
 			// wizard
-			openInstallationWizard();
+/*			openInstallationWizard();
+*/
+                                        zpi.success();
+
 		}
 	}
 	
